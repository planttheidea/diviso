{
  "author": "planttheidea",
  "bugs": {
    "url": "https://github.com/planttheidea/diviso/issues"
  },
  "engines": {
    "node": ">=12.20.0"
  },
  "exports": {
    "./package.json": "./package.json",
    ".": {
      "import": {
        "types": "./esm/index.d.mts",
        "default": "./esm/index.mjs"
      },
      "types": "./index.d.ts",
      "module": "./esm/index.js",
      "default": "./index.js"
    }
  },
  "homepage": "https://github.com/planttheidea/diviso#readme",
  "description": "Shared internals for diviso state manager and its supporting libraries.",
  "keywords": [],
  "license": "MIT",
  "main": "./index.js",
  "name": "diviso-shared",
  "repository": {
    "directory": "packages/diviso-shared",
    "type": "git",
    "url": "git+https://github.com/planttheidea/diviso.git"
  },
  "scripts": {
    "build": "NODE_ENV=production rollup -c ../../rollup.config.js --config-diviso-shared",
    "clean": "shx rm -rf dist",
    "copy": "shx cp -r dist/src dist/esm && shx cp -r dist/*.d.ts dist/esm",
    "dist": "npm run clean && npm run build && npm run copy && npm run patch:esm-ts",
    "lint": "eslint index.ts src",
    "patch:esm-ts": "node -e \"require('shelljs').find('dist/esm/**/*.d.ts').forEach(f=>require('fs').copyFileSync(f,f.replace(/\\.ts$/,'.mts')))\"; shx sed -i \"s/ from '(\\..*)';$/ from '\\$1.mjs';/\" 'dist/esm/**/*.d.mts'; shx sed -i \"s/^declare module '(\\..*)'/declare module '\\$1.mjs'/\" 'dist/esm/**/*.d.mts'"
  },
  "type": "module",
  "types": "./index.d.ts",
  "version": "0.1.0"
}
